import FormulaProp from "./FormulaProp";

/**
 * É uma função executada ao renderizar o componente raiz da página.
 * Feita para testar as classes.
 */
export default function() {
  // escreva seu código aqui ex:
  let formula1 = new FormulaProp();

  console.log("teste 1", formula1.toString())
}